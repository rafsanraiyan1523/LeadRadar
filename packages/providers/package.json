{
  "name": "@lead-radar/providers",
  "version": "0.1.0",
  "private": true,
  "description": "External data provider abstractions (lead discovery, ...) with zero-cost mock/local defaults and optional paid implementations behind the same interface.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "@lead-radar/types": "workspace:*",
    "cheerio": "^1.2.0",
    "undici": "^8.10.0"
  },
  "devDependencies": {
    "@lead-radar/eslint-config": "workspace:*",
    "@types/node": "^20.16.0",
    "eslint": "^9.13.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.4"
  }
}
