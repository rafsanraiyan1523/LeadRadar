# Copy to .env.local and adjust for your local setup.

# Public: exposed to the browser. Used for client-side API calls.
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Server-only: used for server-side (SSR/route handler) API calls.
API_URL="http://localhost:4000"
